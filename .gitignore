node_modules/
dist/
.env
.env.local
*.log
.DS_Store
.next/
out/
# Test-coverage output only — anchored so it does NOT match the app's
# /coverage route folder (Frontend/src/app/(dashboard)/coverage).
/coverage/
Backend/coverage/
Frontend/coverage/
*.tsbuildinfo
